name: Generate GitHub Stats

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  stats:
    permissions:
      contents: write

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate GitHub Stats
        run: |
          mkdir -p stats
          curl -L "https://github-readme-stats.vercel.app/api?username=13eeCoder&show_icons=true&theme=tokyonight" \
            -o stats/github-stats.svg
          curl -L "https://github-readme-stats.vercel.app/api/top-langs/?username=13eeCoder&layout=compact&theme=tokyonight" \
            -o stats/top-languages.svg

      - name: Commit stats
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add stats/
          git diff --cached --quiet || git commit -m "Update GitHub stats"
          git push
